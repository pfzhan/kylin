name: 获取model的shard_columns
request:
  headers:
    Content-Type: application/json;charset=utf-8
    accept: application/vnd.apache.kylin-v4+json
    authorization: Basic $role
  method: GET
  url: /models/${get_model_uuid($resident_model,$project_name)}/agg_indices/shard_columns?project=$project_name