name: public模型元数据导入
variables:
  file_path: '/tmp/temp_model.zip'
  multipart_encoder: ${multipart_encoder($file_path, $resident_model)}
request:
  headers:
    accept: application/vnd.apache.kylin-v4-public+json
    authorization: Basic $role
    Content-Type: ${multipart_content_type($multipart_encoder)}
  data: $multipart_encoder
  method: POST
  url: metastore/import/models?project=$project_name